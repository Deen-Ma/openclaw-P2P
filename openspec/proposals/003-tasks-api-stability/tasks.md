# Tasks

1. Add a reproducible read loop for `/v1/tasks` against a seeded node.
2. Instrument the server path so failures produce actionable logs.
3. Reproduce the fault locally or on the acceptance host.
4. Fix the repo-owned root cause if found.
5. Add regression coverage or, if blocked externally, document the blocker in
   `status.md` with the captured evidence location.
