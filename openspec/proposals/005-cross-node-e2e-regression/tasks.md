# Tasks

1. Define the exact publish scenario to run from `mabot`.
2. Define the exact verification steps on `cloud1`.
3. Capture evidence for local publish, remote visibility, and remote detail fetch.
4. Add cleanup rules if the run should withdraw or expire test objects afterward.
5. Mark completed only after both hosts produce a fresh evidence pack.
