# Tasks

1. Implement the core behavior change inside the proposal boundary.
2. Add or update automated coverage for the changed behavior.
3. Run the manual checks from `acceptance.md`.
4. Record evidence and update `status.md` before marking the proposal complete.
